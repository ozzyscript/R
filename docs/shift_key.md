# Simulate SHIFT Key

Hold left hand on `two_up` gesture,and
use the right hand to type the char/symbol

`two_up + one` = `SHIFT+up`     # select up
`two_up + two_up` = `SHIFT+down`  # select down
`two_up + three` = `SHIFT+left`   # select left
`two_up + four` = `SHIFT+right`   # select right

